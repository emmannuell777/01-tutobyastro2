---
const { url, title, pubDate } = Astro.props;
---

<li class="post-links">
  <a  href={url}>{title}</a>
  {
    pubDate && (
      <div class='s7'>
        <em>{pubDate.toString().slice(0, 10)}</em>
      </div>
    )
  }
</li>
